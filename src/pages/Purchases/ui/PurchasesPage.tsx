import { Stack } from '@mui/material';
import { PurchasesTable } from '@/widgets/PurchasesTable';

const PurchasesPage = () => (
    <Stack spacing={8}>
        <PurchasesTable />
    </Stack>
);

export default PurchasesPage;
