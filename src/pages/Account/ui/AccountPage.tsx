import { Stack } from '@mui/material';
import { AccountForm } from '@/features/user/edit';

const AccountPage = () => (
    <Stack spacing={8}>
        <AccountForm />
    </Stack>
);
export default AccountPage;
